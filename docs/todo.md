# Task 1 当前推进 Todo

总目标
- [ ] 以 Task 1 充电负荷预测为主线，产出可复现、可回测、可导出的稳定方案
- [ ] 先建立强单模 GBDT 基线，再决定是否投入天气特征、更多树模型与深度模型
- [ ] 所有训练、验证、预测、导出流程保持命令行化，并将正式实验产物固定到 `experiments/<exp>/`

执行原则
- [x] 先验证，后冲榜
- [x] 优先保证 RMSE 离线验证与线上目标一致
- [x] 所有主流程脚本化，不依赖 notebook
- [x] 实验结果必须保留配置、fold 指标、预测文件、结论摘要
- [ ] 在统一切分下比较模型，避免一边改切分一边改模型造成结果不可比

一、已完成基础设施与基线能力

1. 规则、数据与工程基础
- [x] 核对评分方式、提交格式、A/B 榜时间窗、公开外部数据边界
- [x] 完成 Task 1 数据审计并输出记录
- [x] 固定目录结构、CLI 入口、配置与输出目录约定
- [ ] 建立实验记录模板

2. 最小验证体系
- [x] 编写 `RMSE` 评估脚本
- [x] 建立 `3-fold rolling-origin backtest`
- [x] 输出每个 fold 指标与 overall 汇总
- [x] 产出基础回测报告 `reports/task1_backtest_overview.md`

3. 已实现 baseline 与特征层
- [x] 跑通 `last_day_same_slot`
- [x] 跑通 `last_7day_same_slot`
- [x] 跑通 `weekday_slot_mean`
- [x] 建立 baseline registry，支持按名称选择模型
- [x] 建立基础时间特征层
- [x] 建立基础 lag 特征层
- [x] 建立基础 rolling 特征层
- [x] 建立 same-weekday-slot 与 same-slot 历史统计特征
- [x] 建立节假日、调休、节前节后标记

4. 已具备但尚未完成首轮正式结果的能力
- [x] 实现 `lightgbm_recursive` baseline 接口与递归推理逻辑
- [x] 实现 `lightgbm_direct` baseline 接口与多步直推逻辑
- [x] 提供 `scripts/run_baseline_experiment.py` 统一实验入口
- [ ] 在 `experiments/<exp>/` 下产出首轮正式 GBDT 实验结果

二、当前最高优先级：跑出首轮 LightGBM 正式实验

1. 固定首轮实验协议
- [ ] 固定输入数据、编码、预测步长、fold 数量与 backtest 切分
- [ ] 固定首轮 `FeatureSpec`，首轮比较时不同时改模型族与特征集合
- [ ] 固定实验目录命名与产物结构，确保每轮实验可追踪

2. 首轮主力对比
- [ ] 跑通 `lightgbm_recursive` 正式实验
- [ ] 跑通 `lightgbm_direct` 正式实验
- [ ] 汇总两者的 fold 指标、overall RMSE 与提交文件统计
- [ ] 明确首轮主力路线优先选 `recursive` 还是 `direct`

3. 提交链路闭环
- [ ] 生成第一版 `LightGBM` 预测结果
- [ ] 导出合法 CSV 提交文件
- [ ] 检查列名、空值、负值、重复时间、非法时段
- [ ] 完成一次从训练数据到提交文件的完整闭环

三、紧随其后：误差分析驱动的第二轮增强

1. 先做误差分析，不盲目扩模型
- [ ] 做工作日 / 周末误差分析
- [ ] 做节假日 / 非节假日误差分析
- [ ] 做峰时 / 谷时 / 平时误差分析
- [ ] 做按 `quarter_slot` 聚合的误差分析
- [ ] 做按预测步长位置聚合的误差分析，重点观察 `recursive` 误差累积
- [ ] 做高负荷日、尖峰日误差分析
- [ ] 输出误差分析文档

2. 第二轮特征增强候选
- [ ] 增加波动性特征，如 `rolling_std`、`rolling_range`
- [ ] 增加趋势差分特征，如 `lag_diff_1d`、`lag_diff_7d`
- [ ] 增加安全的历史日级上下文特征，只使用历史完整日汇总
- [ ] 评估周期编码特征是否有帮助，如 `slot_sin` / `slot_cos`
- [ ] 所有新特征均在统一切分上做增量 A/B 验证

四、第二优先级扩展：更多 GBDT 与外部数据

1. 更多树模型
- [ ] 仅在 `LightGBM` 首轮结果稳定后，引入 `CatBoost direct`
- [ ] 仅在 `LightGBM` 首轮结果稳定后，引入 `XGBoost direct`
- [ ] 保持与 `LightGBM` 相同验证切分进行公平比较
- [ ] 输出第一轮 GBDT 模型离线排行榜

2. 天气公开数据接入
- [ ] 先判断误差形态是否支持引入天气特征
- [ ] 优先接入温度特征
- [ ] 第二阶段接入降雨特征
- [ ] 记录数据源、访问日期、覆盖范围、时间对齐方法
- [ ] 完成缺失处理与 sanity check
- [ ] 对比接入天气前后的真实增益

五、延后项：深度模型、集成与后处理

1. 深度模型
- [ ] 仅在强 GBDT 单模稳定后，再评估是否投入 `PatchTST`
- [ ] 使用与 GBDT 相同的验证切分
- [ ] 判断其是否作为差异性模型进入候选池

2. 集成与后处理
- [ ] 仅在至少 2 个稳定候选模型出现后，再尝试 `mean blend`
- [ ] 再评估 `weighted blend` 与场景加权 blend
- [ ] 仅在有稳定收益时，再做非负裁剪、soft cap、轻度平滑
- [ ] 检查后处理是否破坏峰谷结构

六、工程与复现性
- [ ] 建立实验记录模板，至少包含配置、命令、指标、结论、产物路径
- [ ] 补齐主实验配置文件，而不是只依赖命令行默认参数
- [ ] 补充 README 中 Task 1 运行说明
- [ ] 至少做一次空环境重跑检查
- [ ] 检查训练、验证、导出是否可在无人工干预下完成

七、当前建议执行顺序
- [ ] Step 1: 固定首轮 `LightGBM` 实验协议与 `FeatureSpec`
- [ ] Step 2: 运行 `lightgbm_recursive` 正式实验并保存到 `experiments/<exp>/`
- [ ] Step 3: 运行 `lightgbm_direct` 正式实验并在同一协议下比较
- [ ] Step 4: 产出首轮离线结果汇总与第一版合法 submission
- [ ] Step 5: 基于误差分析决定第二轮特征增强
- [ ] Step 6: 再决定是否扩到 `CatBoost`、`XGBoost`、天气与 `PatchTST`

八、风险提醒
- [ ] 不在验证体系未固定时盲目刷榜
- [ ] 不把高泄漏风险日级汇总字段直接用作一线 baseline 输入
- [ ] 不在没有正式实验产物前同时扩展过多模型路线
- [ ] 不让 notebook 承担正式训练、验证、导出流程
- [ ] 不以单次最好分代替跨 fold 稳定性判断
