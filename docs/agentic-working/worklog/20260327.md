# Worklog 2026-03-27

- 14:47:14 新增 `docs/ProjectBasis.md` 固化项目基础约束、目录规范、实验命名规则与 worklog/impl 文档约定。
- 15:01:42 新增根目录 `AGENTS.md`，基于竞赛说明、数据文件与 `docs/ProjectBasis.md` 固化项目目标、数据约束、建模规则与 agent 工作规范。
- 15:10:00 新增 `docs/CommunicationBasis.md`，固化中英文输入下的交互模式、统一中文输出规则与混合语言处理约定。
- 15:13:00 更新 `AGENTS.md`，要求 agent 同时遵循 `docs/ProjectBasis.md` 的工程约束与 `docs/CommunicationBasis.md` 的交流约定。
- 16:00:10 新增 Task 1 数据审计脚本、报告与实现说明，并将审计产物输出到 `artifacts/00_data_audit/`。
- 16:25:01 修正 `AGENTS.md` 文档优先级顺序与 `docs/ProjectBasis.md` 虚拟环境路径描述。
- 16:27:38 将 `docs/ProjectBasis.md` 与 `docs/CommunicationBasis.md` 全文改写为中文表述，保留原有规则含义与技术字面量。
- 17:53:02 将 Task 1 数据审计接入 `pixi run data-audit` 统一入口，并验证 `pixi` 任务可正常执行。
- 17:56:09 新增 RMSE 与 3-fold rolling-origin 基线回测脚本，接入 `pixi run backtest-baselines` 并产出 `artifacts/01_backtest_baselines/`。
- 18:04:45 重写本地未推送历史以移除被误跟踪的 `docs/ComputeMachine.md`，并修正 `.gitignore` 改为忽略该路径。
- 18:10:30 根据实验目录与根级 `artifacts/` 的职责划分，确认并勾选 `todo.md` 中“固定模型、日志、预测结果输出路径”。
